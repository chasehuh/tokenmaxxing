---
title: Codex support
description: Pooling ChatGPT-subscription accounts for the Codex CLI, per session
---

The same pooling works for OpenAI's Codex CLI, against your own ChatGPT-subscription accounts:

```sh
tokenmaxxing init --codex   # log in the first account, isolated, + install the codex supervisor & Stop hook
tokenmaxxing add --codex    # log in another account, isolated - your primary login is untouched
codex                       # use codex as always
```

The Codex pool is separate state: its own accounts index, its own per-account stores (0600 files on both platforms, since codex's own store is a plaintext file), and its own lock. The engine is shared: one account model (`id`, `label`, `email`, `tier`, cached `windows`), one picker, one decision, and one set of account commands (`init`, `add`, `auth`, `rm`, `rename`, and `status`) run against a Codex provider when you pass `--codex`. The provider holds only what differs: identity from the `auth.json` id token, the usage GET, the credential store, presence, and restart on swap.

## One store per account, sessions shared

Each pooled account owns one store under the state directory: `codex-stores/<uuid8>/`. The store holds that account's `auth.json`; everything else in the store (config, hooks, sessions, sqlite state, skills, caches) is a symlink to the matching entry in your real `~/.codex`, so transcripts, thread history, and settings stay shared and `codex resume <session-id>` works across stores. Only the credential is namespaced. A session runs on an account because the supervisor sets `CODEX_HOME` to that account's store before it spawns codex.

tokenmaxxing writes a store once, when `init`, `add`, or `auth` harvests an isolated login into it. After that the only writer is codex's own token refresh, which runs inside each session against its own store. A credential is never copied between stores: a refresh rotation revokes the superseded refresh token, so two stores holding one grant would kill the grant family.

## Why restart is the switch on Codex

A running codex process holds one in-memory auth manager and explicitly refuses to reload `auth.json` when it now names a different account. There is no file watcher and no per-request credential poll, so a credential swapped underneath a running codex simply never takes effect for inference. Restart is the switch: `codex resume <session-id>` continues the local transcript on whatever account the new process started under.

tokenmaxxing handles this with a codex supervisor shim: the Stop hook runs the same pace-pressure decision against the same two bars at each turn boundary, and when it swaps, it writes a marker that makes the supervisor SIGTERM the session at the committed boundary and relaunch `codex resume <session-id>` under the target's store, transcript intact. A marker the supervisor cannot parse stops the session instead of resuming it. Because every codex switch is a visible restart, the automatic codex decision moves only once the session's account is at or over a bar. There is no depleted-pool countdown for codex - nothing can pause a codex session - so a fully depleted codex pool stays put. When the session's account is at or over a bar, the hook first compacts the thread on that account through `codex app-server` (`thread/resume`, then `thread/compact/start`), so the resumed session on the fresh account starts from the summary rather than re-uploading the whole history; see [Compaction before a move](/switching#compaction-before-a-move).

## Headless `codex exec`

`codex exec` runs as a managed job on this fork: the shim places it on a pooled account before launch (the best usable account with no live session, else the least-loaded usable one), and when the server refuses a turn on a quota limit it walls that seat, moves the job to the best sibling, and relaunches `codex exec resume <thread> <flags> ""` under the sibling's store. When every account is walled the job waits in place for a reset within `policy.headlessMaxWaitMs`, otherwise it exits 75 with a resumable record under `jobs/`. Details: `docs/auto-swap-long-sessions.md`.

Other codex-specific mechanics:

- Usage is read from a free authenticated GET against codex's own rate-limit endpoint (the same call the CLI's `/status` makes): percentages plus absolute epoch reset times, aggregate windows and per-model caps alike. Window classification is duration-driven, never position-driven, because current plans may have no 5-hour window at all and the weekly window sits in the primary slot.
- Codex refresh tokens rotate, and reusing a superseded one kills the whole grant family. Every rotation is persisted the instant it returns into the account's own store, and per-session presence files ensure an account running in a live sibling session is never a placement or move target and never gets its store refreshed from outside.
- Codex requires the file credential store: `init --codex` fails fast if the keyring store is pinned, and `add --codex` writes the file-mode setting into the isolated onboarding home so the login lands harvestable. `auth --codex <sel>` reauthenticates a pooled Codex account in place through the same isolated login and refuses a login that lands on a different account.
- Parked Codex figures refresh when `status` samples the pool; the periodic check tick samples Claude accounts only, so the Codex decision reads whatever figure the last `status` left behind, with a passed reset reading as empty.
- Codex keeps no pool-wide active label, no post-swap cooldown, and no manual `switch`: each codex session runs on its own account's store and moves on its own, so placement at launch replaces the manual pick.
- One manual step per seat: codex trusts hooks per store path, so after `init --codex` open a supervised session on each pooled account, run `/hooks`, and trust the tokenmaxxing Stop hook - auto-switching stays silently inert on an untrusted seat. `tokenmaxxing doctor` lists seats still needing trust, and `init --codex` prints this reminder.
