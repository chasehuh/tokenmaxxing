{
  "name": "tokenmaxxing",
  "version": "1.50.0",
  "description": "Automatic Claude Code account switching: pool multiple accounts, start each session on its own account, and resume a session on a fresher account when quota fills.",
  "type": "module",
  "license": "MIT",
  "author": "anaclumos",
  "homepage": "https://github.com/anaclumos/tokenmaxxing",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/anaclumos/tokenmaxxing.git"
  },
  "bin": {
    "tokenmaxxing": "./src/main.ts"
  },
  "files": [
    "src",
    "README.md",
    "DESIGN.md"
  ],
  "engines": {
    "bun": ">=1.3.6"
  },
  "os": [
    "darwin",
    "linux"
  ],
  "scripts": {
    "dev": "bun run src/main.ts",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run typecheck && bun run test",
    "nix:bun": "bunx bun2nix@2.1.2 -o bun.nix",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "es-toolkit": "^1.49.0",
    "ky": "^2.0.2",
    "zod": "^4.4.3"
  }
}
